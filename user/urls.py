from django.urls import path, re_path
from django.conf.urls import url

from user.views import *
# from service.apis.webhook import webhook_braintree 

urlpatterns = [
	# url(r'^user$', UserApiView.as_view(), name='user_crud'),
]